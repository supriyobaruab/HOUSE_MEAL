import { post } from "./postInfo.js";
export async function supriyo(name) {
  const amount = prompt("Enter the amount");
  await post(name, amount);
}
export async function debongshi(name) {
  const amount = prompt("Enter the amount");
  await post(name, amount);
}
export async function waly(name) {
  const amount = prompt("Enter the amount");
  await post(name, amount);
}
export async function mahmud(name) {
  const amount = prompt("Enter the amount");
  await post(name, amount);
}
